import { createAction } from "redux-actions";

export const openModal = createAction("@ADD_SCREENING_DATE_MODAL/OPEN_MODAL");

export const closeModal = createAction("@ADD_SCREENING_DATE_MODAL/CLOSE_MODAL");
